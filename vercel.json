{
  "functions": { "./server.js": { "memory": 1024 } },
  "buildCommand": "yarn --cwd client && yarn --cwd clien build",
  "routes": [
    {
      "src": "/(.*)",
      "dest": "server.js"
    }
  ]
}
