{
  "name": "tacos-creator",
  "version": "3.0.0",
  "description": "create and view taco recipes",
  "main": "server.js",
  "scripts": {
    "start": "node server",
    "server": "nodemon server",
    "client": "npm start --prefix client",
    "build": "yarn --cwd client && yarn --cwd client build",
    "dev": "concurrently \"npm run server\" \"npm run client\""  
  },
  "author": "Marco Wong",
  "license": "ISC",
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "config": "^3.3.0",
    "express": "^4.17.1",
    "express-validator": "^6.4.0",
    "jsonwebtoken": "^8.5.1",
    "mongoose": "^5.9.6"
  },
  "devDependencies": {
    "concurrently": "^5.1.0",
    "nodemon": "^2.0.2"
  }
}
